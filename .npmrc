# ============================================
# Project-specific pnpm configuration
# ============================================
# This file overrides global settings from C:\Users\kevin\.pnpmrc
# Only include project-specific settings here

# Project uses legacy peer deps for some packages
# (Global config already handles most settings)

# Approve build scripts for these dependencies
onlyBuiltDependencies=@sentry/cli,esbuild,puppeteer,sharp,unrs-resolver
